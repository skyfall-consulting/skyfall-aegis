/**
 * @deprecated Use `TextField` instead. `Input` will be removed in a future major version.
 *
 * ```tsx
 * // Before
 * import { Input } from '@skyfall_ai/aegis';
 *
 * // After
 * import { TextField } from '@skyfall_ai/aegis';
 * ```
 */
export { TextField as Input } from '../TextField/TextField';
export type { TextFieldProps as InputProps } from '../TextField/TextField';
