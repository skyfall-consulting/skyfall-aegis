/**
 * @deprecated Use `AppBar` instead. `TopNav` will be removed in a future major version.
 *
 * ```tsx
 * // Before
 * import { TopNav } from '@skyfall_ai/aegis';
 *
 * // After
 * import { AppBar } from '@skyfall_ai/aegis';
 * ```
 */
export { AppBar as TopNav } from '../AppBar/AppBar';
export type { AppBarProps as TopNavProps } from '../AppBar/AppBar';
